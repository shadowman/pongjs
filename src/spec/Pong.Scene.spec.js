describe('Scene', function() {
	var scene;

	beforeEach(function() {
		scene = new Scene();
	});
	
	it('should have a collection of nodes', function() {
		fail();
	});

	it('should create new nodes', function() {
		fail();
	});

	it('should have a root node', function() {
		fail();
	});

	it('should added nodes should go to the nodes collection', function() {
		fail();

	});

	it('should visit all nodes in the hierarchy visit gets called', function() {
		fail();
	});
});

describe('SceneNode', function() {
	it('should let you attach new nodes to him', function() {
		fail();
	});

	it('should let you remove nodes from him', function() {
		fail();
	});

	it('should let you see all its children', function() {
		fail();
	});

	it('should visit all of his children with the visitor when visit gets called', function() {
		fail();
	});
});