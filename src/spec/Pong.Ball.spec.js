describe('Ball', function(){
	var ball;

	beforeEach(function() {
		ball = new Ball();	
	});

	it('should initialize its position in the center');

	it('should initialize its velocity vector with a random value');
	
	it('should render itself as a circle when render gets called');

});